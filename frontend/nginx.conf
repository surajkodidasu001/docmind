server {
    listen 80;
    root /usr/share/nginx/html;
    index index.html;

    # SPA fallback: any unmatched path serves index.html so client-side
    # routing (if added later) doesn't 404 on refresh
    location / {
        try_files $uri $uri/ /index.html;
    }
}
